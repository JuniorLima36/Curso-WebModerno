/* 03) Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao 
expoente. */

function expo(x, y) {
    console.log('Método antigo - (**) =', result = Math.pow(x, y))
    console.log('Método Novo - (Math.pow) =', result = x ** y)
}
expo(5, 6)